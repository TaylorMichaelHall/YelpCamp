#intro to node

*what is node?
*Why are we learning it?
    *It's popular!
    *it's javascript!
*(it doesn't matter(long term))

----

218
Using Node

execute a file in node like this:
node <filename>